# aiteacherguard 백엔드 의존성
fastapi>=0.111
uvicorn[standard]>=0.30
sqlalchemy>=2.0
pydantic>=2.7
httpx>=0.27
# Postgres + pgvector (docker-compose 사용 시)
psycopg[binary]>=3.1
pgvector>=0.3
# 기본 DB는 SQLite라 위 psycopg 없이도 로컬 실행 가능
